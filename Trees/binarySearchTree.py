"""
BST Use:
Can be used as internal implementation of Set(), we cannot insert duplicate in BST
useful for searching because everytime we search , it reduce the searching space by half

It has atmost two child nodes

searching complexity = O(logn)
insertion complexity = O(logn)

"""
